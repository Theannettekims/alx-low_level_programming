#include <stdio.h>
#include "main.h"

/**
 * main - prints name + \n
 * @argv: array pointing to arguments
 * @argc: number of arguments typed
 * Return: 0
 */

int main(int argc, char *argv[])
{
	argc = 0;
	printf("%s\n",argv[argc]);

	return (0);
}
