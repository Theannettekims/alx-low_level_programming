#include <stdio.h>
#include "main.h"

/**
 * main - prints name + \n
 * @argv: pointer to arguments
 * @argc: command line arguments
 * Return: 0 success
 */

int main(int argc, char *argv[])
{
	argc = 0;
	printf("%s\n",argv[0]);

	return (0);
}
